{
    "name": "server",
    "version": "1.0.0",
    "scripts": {
        "server": "cd ../client && npm run convert && cd ../server && NODE_ENV=production DEBUG=engine:* node --use_strict src/boot.js",
        "dev": "NODE_ENV=development DEBUG=engine:* node --use_strict src/boot.js"
    },
    "dependencies": {
        "babel-preset-env": "^1.7.0",
        "babel-register": "^6.26.0",
        "debug": "^3.1.0",
        "express": "^4.16.3",
        "pug": "^2.0.3"
    }
}